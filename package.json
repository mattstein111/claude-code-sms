{
  "name": "claudecode-sms",
  "version": "0.4.0-beta",
  "description": "Claude Code channel plugin for two-way SMS/MMS with pluggable provider support",
  "type": "module",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "server": "bun run server.ts",
    "listener": "bun run listener.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
